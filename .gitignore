# Anything a player of the studio might export or save locally.
# These can contain uploaded photos/audio and should never be committed.
*.fnafproj
/exports/
/dist/
/*.zip
/*.html
!index.html

# Installer build artifacts (shipped as release assets, not in the repo)
installer/out/

# OS/editor noise
.DS_Store
Thumbs.db
